live-boot
live-config

# The X server and its video drivers are NOT listed here -- XLibre replaces
# Xorg, and its packages come from a third-party HTTPS repo that cannot be
# reached this early in the build. 040-xlibre.chroot installs them instead; see
# the comment at the top of that hook.
mesa-utils
mesa-vulkan-drivers
libva2

# Needed for Network prefPane
network-manager
iw
wireless-tools

# Needed for Audio prefPane
alsa-utils

# Everything marked with "#x86_64" (uname -m" will be installed on that architecture only
#x86_64 intel-media-va-driver-non-free 

# Proprietary Nvidia drivers
# https://wiki.debian.org/NvidiaGraphicsDrivers#Version_535.183.01-1
# Since these fail to install from here, we are doing it in
# a script...

#aarch64 raspi-firmware
parted

qemu-guest-agent
spice-vdagent

