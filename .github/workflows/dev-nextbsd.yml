name: dev-nextbsd

# Reference flavor for the monorepo: build -> gate -> publish, on EVERY run.
# The build is the only flavor-specific bit (inline, in a FreeBSD VM). Everything
# else is the shared layer every flavor calls: free-disk, screenshot-gate (boot
# + greeter login + desktop-render check, init-agnostic), publish-continuous.
#
# Trigger: this flavor's target (targets/nextbsd/**) or shared actions (.github/actions/**);
# README and other top-level files never build. There is NO "reuse the last ISO"
# fast path — the gate always boots the ISO this run just built, so it can never
# test an image that doesn't exist.

on:
  push:
    branches: [main]
    paths: ['targets/nextbsd/**', '.github/actions/**', '.github/workflows/dev-nextbsd.yml']
  pull_request:
    paths: ['targets/nextbsd/**', '.github/actions/**', '.github/workflows/dev-nextbsd.yml']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: build (nextbsd amd64)
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/free-disk

      - name: Resolve gershwin-developer ref (dev if it exists, else main)
        id: gd
        run: |
          if git ls-remote --exit-code --heads https://github.com/gershwin-desktop/gershwin-developer.git dev >/dev/null 2>&1; then
            echo "ref=dev" >> "$GITHUB_OUTPUT"; echo "gershwin-developer: using dev branch"
          else
            echo "ref=main" >> "$GITHUB_OUTPUT"; echo "gershwin-developer: no dev branch — using main"
          fi

      - name: Build ISO inside FreeBSD VM
        uses: vmactions/freebsd-vm@v1
        with:
          release: "15.1"
          usesh: true
          sync: rsync
          copyback: true
          cpu: 4
          mem: 8192
          prepare: |
            pkg install -y git
          run: |
            set -e
            export TARGET_ARCH=amd64
            export CHANNEL=dev
            export GERSHWIN_REF=${{ steps.gd.outputs.ref }}
            export GERSHWIN_BRANCH=dev
            /bin/sh ./targets/nextbsd/build.sh
            ls -lh /usr/local/gon-build/iso/
            mkdir -p out
            cp /usr/local/gon-build/iso/*.iso out/

      - uses: actions/upload-artifact@v4
        with:
          name: gershwin-nextbsd-amd64
          path: out
          retention-days: 1
          if-no-files-found: error

  test:
    name: boot + login + screenshot (nextbsd amd64)
    needs: [build]
    if: ${{ needs.build.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Download fresh build artifact
        uses: actions/download-artifact@v4
        with:
          name: gershwin-nextbsd-amd64
          path: artifacts

      # The whole boot -> greeter -> admin login -> desktop-render gate lives in
      # the shared action. It observes the Gershwin greeter/desktop from outside
      # the guest, so it is identical for every flavor (init-agnostic). A failure
      # here fails the job and blocks publish (needs.test.result == 'success').
      - uses: ./.github/actions/screenshot-gate
        with:
          flavor: nextbsd
          iso-dir: artifacts
          arch: amd64
          soft-fail: true

  # Diagnostic only: boots the SAME ISO a second time with the kernel console
  # unmuted, and keeps the transcript. It gates nothing -- publish does not
  # depend on it and it cannot fail the run.
  #
  # It exists because the screenshot gate can say "no graphical screen within
  # 600s" and nothing more: the shipped image sets boot_mutemsgs="YES"
  # (nextbsd#363), so its serial log stops at "-- Muting boot messages --" and
  # every kext attach, DRM probe and launchd message after early init is
  # invisible. Diagnosing the dev image that has been booting to a text console
  # meant reading 67 framebuffer dumps to notice 65 were identical.
  #
  # RB_VERBOSE bypasses that mute in the kernel, which is what
  # nextbsd/nextbsd's tests/iso-boot-test.sh already relies on; this runs the
  # same loader handshake.
  #
  # nextbsd only, on purpose. The loader dance and the mute it works around are
  # FreeBSD/NextBSD specific, and putting this in the shared screenshot-gate
  # would rebuild every flavor's ISO on every change to it.
  verbose-boot:
    name: verbose boot log (nextbsd amd64, diagnostic)
    needs: [build]
    if: ${{ needs.build.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Download fresh build artifact
        uses: actions/download-artifact@v4
        with:
          name: gershwin-nextbsd-amd64
          path: artifacts

      - name: Install qemu + ovmf + expect
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86 ovmf expect

      - name: Boot verbosely and capture the console
        run: |
          ISO=$(ls artifacts/*.iso 2>/dev/null | head -1)
          [ -n "$ISO" ] || { echo "no ISO in artifacts"; ls -laR artifacts; exit 0; }
          sh targets/nextbsd/tests/verbose-boot.sh "$ISO"

      - name: Upload verbose boot log
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: nextbsd-verbose-boot-log
          path: tests/verbose-boot.log
          retention-days: 7
          if-no-files-found: ignore

  publish:
    name: publish nextbsd-dev
    # Public release is gated on boot + login + screenshot all passing.
    needs: [build, test]
    if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main' && needs.build.result == 'success' && needs.test.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download ISO
        uses: actions/download-artifact@v4
        with:
          name: gershwin-nextbsd-amd64
          path: dist

      - name: Download screenshot
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: desktop-screenshot
          path: screenshot

      - uses: ./.github/actions/publish-continuous
        with:
          tag: nextbsd-dev
          title: NextBSD (dev)
          assets-dir: dist
          screenshot: screenshot/gershwin-on-nextbsd.png
          token: ${{ secrets.GITHUB_TOKEN }}

  cleanup:
    name: purge run ISO artifact
    needs: [build, test, publish]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Delete this run's ISO/screenshot artifacts (job-handoff only; ISO is on the release)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api "/repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/artifacts" \
            --jq '.artifacts[] | select(.name != "boot-artifacts") | .id' \
          | while read -r id; do
              gh api -X DELETE "/repos/$GITHUB_REPOSITORY/actions/artifacts/$id" \
                && echo "deleted artifact $id"
            done
